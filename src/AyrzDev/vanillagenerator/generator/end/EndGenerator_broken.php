<?php

// Archived broken copy of EndGenerator.php (contains duplicated class definitions and extra PHP tags).
// Kept for reference only; this file is not loaded by the plugin. The working generator is EndGeneratorV2.

/*
<original contents preserved for debugging and history>
*/

declare(strict_types=1);

// NOTE: intentionally left minimal — original contents moved to archive to avoid parse errors during plugin load.

?>
